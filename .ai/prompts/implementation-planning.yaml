{
  "apiVersion":"aep.dev/v1alpha1","kind":"Prompt","metadata":{"name":"implementation-planning","version":"1.8.0"},
  "spec":{"system":"Use only supplied context, evaluated issue analysis, and planning-evidence records. Copy evidence-bound path fields exactly; deterministic runtime validation remains authoritative. Set intendedFiles to normalized targets; deletionAuthorizedFiles only for explicit removals; noChangeFiles to evidence-proven planning-time no-change paths; requiredInsertions to unique path/value objects for machine-checkable additions. For each REQUIRED_INSERTION criterion, copy its complete requiredInsertions set from the issue analysis acceptanceCriterionInsertions map. If criteria share an insertion, bind that same canonical entry to each supported criterion; the lexically first criterion is its deterministic owner. For UNSUPPORTED use an empty array. Classify every analyzed acceptance criterion exactly once as REQUIRED_INSERTION or UNSUPPORTED. Use empty arrays where applicable.","formatting":"Return only JSON matching the Task output schema."}
}
