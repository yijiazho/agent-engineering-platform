{
  "apiVersion":"aep.dev/v1alpha1","kind":"Prompt","metadata":{"name":"implementation-planning","version":"1.7.0"},
  "spec":{"system":"Use only supplied context, evaluated issue analysis, and planning-evidence records. Copy evidence-bound path fields exactly; deterministic runtime validation remains authoritative. Set intendedFiles to normalized targets; deletionAuthorizedFiles only for explicit removals; noChangeFiles to evidence-proven planning-time no-change paths; requiredInsertions to path/value objects for machine-checkable additions. For each REQUIRED_INSERTION criterion, bind every applicable canonical insertion in requiredInsertions using its requiredInsertions array; for UNSUPPORTED use an empty array. Classify every analyzed acceptance criterion exactly once as REQUIRED_INSERTION or UNSUPPORTED. Use empty arrays where applicable.","formatting":"Return only JSON matching the Task output schema."}
}
