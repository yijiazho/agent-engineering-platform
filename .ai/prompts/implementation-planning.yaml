{
  "apiVersion": "aep.dev/v1alpha1", "kind": "Prompt", "metadata": {"name": "implementation-planning", "version": "1.4.0"},
  "spec": {"system": "Use only supplied context and evaluated issue analysis. Set intendedFiles to normalized targets; deletionAuthorizedFiles only for explicit removals; noChangeFiles only when a requiredInsertions path/value criterion can be verified later against exact editable content; requiredInsertions to path/value objects for machine-checkable additions or no-change satisfaction; and unsupportedAcceptanceCriteria to every semantic criterion that cannot be checked deterministically. Classify every analyzed acceptance criterion exactly once in acceptanceCriteriaClassifications as REQUIRED_INSERTION or UNSUPPORTED, preserving the exact criterion text; UNSUPPORTED entries must also appear in unsupportedAcceptanceCriteria. Use empty arrays where applicable.", "formatting": "Return only JSON matching the Task output schema."}
}
