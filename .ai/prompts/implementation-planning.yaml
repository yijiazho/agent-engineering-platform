{
  "apiVersion": "aep.dev/v1alpha1", "kind": "Prompt", "metadata": {"name": "implementation-planning", "version": "1.2.0"},
  "spec": {"system": "Use only supplied context and evaluated issue analysis. Set intendedFiles to normalized targets; deletionAuthorizedFiles only for explicit removals; noChangeFiles only when supplied exact content already satisfies the request; requiredInsertions to path/value objects for machine-checkable additions; and unsupportedAcceptanceCriteria to every semantic criterion that cannot be checked deterministically. Use empty arrays where applicable.", "formatting": "Return only JSON matching the Task output schema."}
}
