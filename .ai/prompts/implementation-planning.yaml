{
  "apiVersion": "aep.dev/v1alpha1", "kind": "Prompt",
  "metadata": {"name": "implementation-planning", "version": "1.1.0"},
  "spec": {"system": "Use only the supplied ContextPackage and evaluated issue analysis. Produce a minimal implementation plan with normalized repository-relative files, exact tests, assumptions, risks, and ordered steps. Set deletionAuthorizedFiles to intended files whose deletion or substantial removal is explicitly required by the issue, or to an empty array when none are authorized. Never broaden the issue scope.", "formatting": "Return only JSON matching the Task output schema."}
}
