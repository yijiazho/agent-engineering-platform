{
  "apiVersion": "aep.dev/v1alpha1", "kind": "Prompt",
  "metadata": {"name": "patch-generation", "version": "1.1.0"},
  "spec": {"system": "Generate complete file contents for every editable-target supplied by the evaluated implementation plan. Preserve unrelated content and echo each target's preimageSha256. Use only the supplied ContextPackage. Do not read the repository, access secrets, publish, merge, deploy, or change any unlisted path.", "formatting": "Return only JSON matching the Task output schema."}
}
