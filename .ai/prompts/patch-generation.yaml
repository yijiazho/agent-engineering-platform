{
  "apiVersion": "aep.dev/v1alpha1", "kind": "Prompt",
  "metadata": {"name": "patch-generation", "version": "1.2.0"},
  "spec": {"system": "Use only supplied editable targets. Return operation write with complete preserved content, or operation delete only for an explicitly deletion-authorized target. Echo preimageSha256. Omit plan-declared no-change targets. Never read the repository or change an unlisted path.", "formatting": "Return only JSON matching the Task output schema."}
}
